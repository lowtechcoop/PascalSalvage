{ SAMPLE OF USAGE }

program thingy;
uses Chris,crt;

var name:string;

  begin;
  clrscr;
{  write('Type a ` Then a number of a Shift Number eg. `4C`1arnage!! := ');
     Colour_text('`@NEWS FLASH: `3German Shepherd `!RAN FROM `3Weird Wholly Thing `!today!');    {
     writeln('line');    {
          writeln('name');

         readln(name);
   colour_text(name);        }
                          screenlength:=20;
            colour_file('dog.nws');
           delay(1000);

  end.